name: nova_core
description: Aplikasi NOVA CORE
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  flutter_animate: ^4.3.0

flutter:
  uses-material-design: true
